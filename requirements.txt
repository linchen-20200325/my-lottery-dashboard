# v6.10.1 hotfix:revert v6.9 A4 `~=` 鎖,回到 v6.8 的 `>=` 寬鬆 pin。
# 原因:Streamlit Cloud 安裝失敗(Error installing requirements)— `~=1.39.0`
# 強鎖 streamlit minor 對撞 Cloud runtime 已預載新版的 transitive 相依鏈。
# 教訓:沒有在 Streamlit Cloud 同款 Python 環境驗證過的版本鎖、不該推。
# 若未來要再嘗試 pin,先在 Cloud 同款 Python(3.10/3.11)用 `pip install --dry-run`
# 跑過完整 resolver、確認 transitive 無衝突再說。
streamlit>=1.39
# Below are offline-only deps for src/scraper (NOT imported by streamlit_app.py).
# The live app reads `data/lotto649.csv` committed to the repo, or accepts
# manual upload via the UI; it never makes outbound API calls.
requests>=2.31
urllib3>=2.0
