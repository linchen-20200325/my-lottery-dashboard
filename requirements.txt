# v6.9 (CLAUDE.md §5 可重現性):`~=` 相容版本鎖 — 鎖死 minor、允許 patch。
# 用 `~=` 而非 `==` 因為:
#   1. 此 sandbox 無 streamlit 可 `pip freeze`,無法驗證 Cloud 實裝 patch 版;
#   2. `~=1.39.0` 等價 `>=1.39.0,<1.40` — 比舊 `>=1.39` 緊很多、又允許 Cloud
#      自選同 minor 的安全 patch(避免 Cloud 重建時抓不到 pin 版本而 fallback)。
# 如要 100% bit-for-bit 重現,改用 lockfile(pip-tools / uv)而非直接 `==`。
streamlit~=1.39.0
# Below are offline-only deps for src/scraper (NOT imported by streamlit_app.py).
# The live app reads `data/lotto649.csv` committed to the repo, or accepts
# manual upload via the UI; it never makes outbound API calls.
requests~=2.33.1
urllib3~=2.6.3
